# CodeGraph_RAG
Solves a Real-World Problem: Basic chunking breaks code context (e.g., splitting a class in half across two chunks). By using ast parsing, your system respects structural boundaries (functions, classes, scope).
